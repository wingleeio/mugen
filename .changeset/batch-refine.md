---
"@wingleeio/mugen": patch
---

`refineKeys(keys)` — batch-resolve estimated heights with ONE notification. Per-row `ensureMeasured` inside a scroll path is a notification storm (a re-render per row per event).
