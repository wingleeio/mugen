///
/// HybridMugenTextBlockComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridMugenTextBlockComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::pretextcore::views {

  extern const char HybridMugenTextBlockComponentName[] = "MugenTextBlock";

  HybridMugenTextBlockProps::HybridMugenTextBlockProps(const react::PropsParserContext& context,
                                                       const HybridMugenTextBlockProps& sourceProps,
                                                       const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    spec([&]() -> CachedProp<MugenTextBlockSpec> {
      try {
        const react::RawValue* rawValue = rawProps.at("spec", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.spec;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<MugenTextBlockSpec>::fromRawValue(*runtime, value, sourceProps.spec);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MugenTextBlock.spec: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridMugenTextBlockSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridMugenTextBlockSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("MugenTextBlock.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridMugenTextBlockProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("spec"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridMugenTextBlockComponentDescriptor::HybridMugenTextBlockComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser()) {}

  std::shared_ptr<const react::Props> HybridMugenTextBlockComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                          const std::shared_ptr<const react::Props>& props,
                                                                                          react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridMugenTextBlockShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridMugenTextBlockComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridMugenTextBlockShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridMugenTextBlockProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridMugenTextBlockProps>& props = std::const_pointer_cast<HybridMugenTextBlockProps>(constProps);
    HybridMugenTextBlockState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::pretextcore::views
