///
/// HybridPretextCoreSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `HybridPreparedTextSpec` to properly resolve imports.
namespace margelo::nitro::pretextcore { class HybridPreparedTextSpec; }
// Forward declaration of `HybridPreparedRichInlineSpec` to properly resolve imports.
namespace margelo::nitro::pretextcore { class HybridPreparedRichInlineSpec; }
// Forward declaration of `RichInlineItemSpec` to properly resolve imports.
namespace margelo::nitro::pretextcore { struct RichInlineItemSpec; }

#include <string>
#include <NitroModules/ArrayBuffer.hpp>
#include <memory>
#include "HybridPreparedTextSpec.hpp"
#include "HybridPreparedRichInlineSpec.hpp"
#include "RichInlineItemSpec.hpp"
#include <vector>

namespace margelo::nitro::pretextcore {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `PretextCore`
   * Inherit this class to create instances of `HybridPretextCoreSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridPretextCore: public HybridPretextCoreSpec {
   * public:
   *   HybridPretextCore(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridPretextCoreSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridPretextCoreSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridPretextCoreSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void registerFont(const std::string& family, double weight, const std::string& style, const std::shared_ptr<ArrayBuffer>& data) = 0;
      virtual void clearRegisteredFonts() = 0;
      virtual void setGenericFontFamily(const std::string& generic, const std::string& family) = 0;
      virtual void setEmojiAdvanceEm(double value) = 0;
      virtual double measureTextWidth(const std::string& text, const std::string& font) = 0;
      virtual std::shared_ptr<HybridPreparedTextSpec> prepare(const std::string& text, const std::string& font, bool withSegments, const std::string& whiteSpace, const std::string& wordBreak, double letterSpacing) = 0;
      virtual std::shared_ptr<HybridPreparedRichInlineSpec> prepareRichInline(const std::vector<RichInlineItemSpec>& items) = 0;
      virtual void clearCache() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "PretextCore";
  };

} // namespace margelo::nitro::pretextcore
