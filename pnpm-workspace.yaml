packages:
  - "apps/*"
  - "packages/*"

# pnpm 10 blocks dependency build scripts by default; allow the native
# toolchains the docs app needs to compile.
onlyBuiltDependencies:
  - esbuild
  - "@tailwindcss/oxide"
  - unrs-resolver
